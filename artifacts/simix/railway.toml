[build]
buildCommand = "pnpm install && pnpm --filter @workspace/simix run build"

[deploy]
startCommand = "pnpm --filter @workspace/simix run serve"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
