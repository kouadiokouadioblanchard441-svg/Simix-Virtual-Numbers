{
  "name": "simix",
  "version": "1.0.0",
  "license": "MIT",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "start": "node --enable-source-maps dist/index.cjs",
    "build": "pnpm --filter @workspace/api-server run build && NODE_ENV=production pnpm --filter @workspace/simix run build",
    "dev:api": "cd artifacts/api-server && PORT=3000 NODE_ENV=development pnpm run build && node --enable-source-maps ../../dist/index.cjs",
    "dev:front": "cd artifacts/simix && pnpm run dev"
  },
  "packageManager": "pnpm@10.26.1",
  "private": true,
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
